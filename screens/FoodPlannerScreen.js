import { Text, View } from "react-native";
import { SafeAreaView } from 'react-native-safe-area-context';
const FoodPlannerScreen = () => {
    return (
      <SafeAreaView>
        <Text>Food Planner</Text>
      </SafeAreaView>
    );
}

export default FoodPlannerScreen;